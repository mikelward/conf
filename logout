# .logout - C Shell login session shutdown commands
# $Id$

# kill ssh-agent
if ( $?SSH_AGENT_PID ) then
    ssh-agent -k
endif

# clear the screen
clear

# read local settings (company environment, etc.)
if ( -r ~/.logout.local ) then
    source ~/.logout.local
endif

