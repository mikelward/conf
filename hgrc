[pager]
pager = less -FRX

[templates]
shortnode = "{label('log.changeset', shortest(node, 7))}"
shortdate = "{label('log.date', date|shortdate)}"
shortdesc = "{desc|firstline}"
oneline = "{shortnode} {shortdate} {shortdesc}\n"

[ui]
ignore = ~/.hgexclude
merge = meld

%include .hgrc.local
