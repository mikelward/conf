# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 1
}

order += "wireless _first_"
order += "ethernet _first_"
order += "volume headphones"
order += "volume master"
order += "battery all"
order += "tztime local"

wireless _first_ {
        format_up = "%essid %quality"
        format_down = "down"
}

ethernet _first_ {
        # if you use %speed, i3status requires root privileges
        format_up = "up"
        format_down = "down"
}

battery all {
        format = "%status %percentage"
        integer_battery_capacity = true
        status_chr = "⚡"
        status_bat = "🔋"
        status_unk = "?"
}

volume master {
        format = "♪ %volume"
        format_muted = "♪ muted"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}

volume headphones {
        format = "🎧"
        format_muted = "%devicename"
        device = "pulse:bluez_sink.CC_98_8B_A8_30_01.headset_head_unit"
}

tztime local {
        format = "%H:%M:%S"
}

tztime london {
        format = "%H:%M %Z"
        timezone = "Europe/London"
        hide_if_equals_localtime = true
}

tztime sunnyvale {
        format = "%H:%M %Z"
        timezone = "America/Los_Angeles"
        hide_if_equals_localtime = true
}

load {
        format = "%1min"
}

disk "/" {
        format = "%avail"
}
